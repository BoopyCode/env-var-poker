# ENV Var Poker

Pokes your environment variables to find missing or empty ones before runtime. Create a JSON file with required variable names, then run: `python env_poker.py required.json [.env_file]`